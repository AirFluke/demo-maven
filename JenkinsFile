pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }

        stage('Hi') {
            steps {
                echo 'Hi'
            }
        }

        stage('Testing JenkinsFile') {
        when{
            expression{
                env.BRANCH_NAME=="feature5"
            }
        }
            steps {
                echo 'Hey there'
            }
        }
        stage("Git checkout"){
            steps{
                checkout changelog: false, poll: false, scm: scmGit(branches: [[name: '*/$BRANCH_NAME']], extensions: [], gitTool: 'Default', userRemoteConfigs: [[credentialsId: 'git_login', url: 'https://github.com/AirFluke/demo-maven.git']])
                bat "echo $CHOICES $BRANCH_NAME"
                bat "dir"
            }
        }
        stage('Testing Hooks') {
            steps {
                echo 'Hook Tested Suucess there'
            }
        }
    }
}
